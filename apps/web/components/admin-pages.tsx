"use client";
import {useMemo,useState} from "react";
import {useDemo,type DemoStudent} from "./demo-provider";
import type {PartnershipStatus} from "@/lib/admin-policy";
const statusLabel:Record<string,string>={active:"Activo",pending:"Pendiente",suspended:"Suspendido",rejected:"Rechazado",revoked:"Revocado",inactive:"Inactivo",archived:"Archivado"};
const badge=(status:string)=>`chip ${status==="active"?"bg-emerald-50 text-emerald-800":status==="pending"?"bg-amber-50 text-amber-900":"bg-slate-100 text-slate-700"}`;
const money=(minor:number)=>new Intl.NumberFormat("es-DO",{style:"currency",currency:"DOP"}).format(minor/100);

export function SchoolDashboard(){const {state,resetDemo}=useDemo(),a=state.administration;return <><Header eyebrow="Administración escolar" title={a.school.name} copy="Estado activo · El colegio conserva la autoridad sobre identidad, padrón y acceso estudiantil."/><Stats values={[["Estudiantes",state.students.length],["Activos",state.students.filter(s=>s.status==="active").length],["Inactivos",state.students.filter(s=>s.status==="inactive").length],["Archivados",state.students.filter(s=>s.status==="archived").length],["Cafeterías activas",a.partnerships.filter(p=>p.status==="active"&&p.schoolName===a.school.name).length],["Solicitudes pendientes",a.partnerships.filter(p=>p.status==="pending"&&p.schoolName===a.school.name).length]]}/><section className="mt-5 grid gap-5 lg:grid-cols-2"><Activity/><div className="card p-5"><h2 className="text-xl font-black">Acciones rápidas</h2><p className="mt-2 text-sm text-slate-600">Importa el padrón, revisa solicitudes o restaura el estado ficticio completo.</p><div className="mt-4 flex flex-wrap gap-2"><a className="btn" href="/admin/escuela/estudiantes">Administrar estudiantes</a><a className="btn-secondary" href="/admin/escuela/cafeterias">Revisar conexiones</a><button className="btn-secondary" onClick={()=>confirm("¿Restaurar todo el demo interconectado?")&&resetDemo()}>Restablecer demo</button></div></div></section></>}
export function CafeteriaDashboard(){const {state,resetDemo}=useDemo(),a=state.administration;return <><Header eyebrow="Administración de cafetería" title={a.cafeteria.name} copy={`Estado activo · ${a.cafeteria.location} · catálogo, personal y conexiones autorizadas.`}/><Stats values={[["Productos disponibles",state.menuItems.filter(i=>i.available).length],["No disponibles",state.menuItems.filter(i=>!i.available).length],["Personal POS activo",a.users.filter(u=>u.role==="pos_operator"&&u.status==="active").length],["Escuelas conectadas",a.partnerships.filter(p=>p.status==="active"&&p.cafeteriaName===a.cafeteria.name).length],["Solicitudes pendientes",a.partnerships.filter(p=>p.status==="pending").length],["Compras recientes",state.purchases.length]]}/><section className="mt-5 grid gap-5 lg:grid-cols-2"><Activity/><div className="card p-5"><h2 className="text-xl font-black">Menú y operación de hoy</h2><p className="mt-2 text-sm text-slate-600">{state.menuItems.length} productos · {state.purchases.length} compras demo completadas. Los cambios del menú se comparten con Estudiante y POS.</p><div className="mt-4 flex flex-wrap gap-2"><a className="btn" href="/admin/cafeteria/menu">Administrar menú</a><a className="btn-secondary" href="/admin/cafeteria/transacciones">Ver transacciones</a><button className="btn-secondary" onClick={()=>confirm("¿Restaurar todo el demo interconectado?")&&resetDemo()}>Restablecer demo</button></div></div></section></>}
function Header({eyebrow,title,copy}:{eyebrow:string;title:string;copy:string}){return <header><span className="label">{eyebrow}</span><h1 className="mt-2 text-3xl font-black">{title}</h1><p className="mt-2 text-slate-600">{copy}</p></header>}
function Stats({values}:{values:(string|number)[][]}){return <section className="mt-5 grid grid-cols-2 gap-3 lg:grid-cols-4">{values.map(([label,value])=><article className="card p-4" key={label}><p className="text-sm text-slate-500">{label}</p><p className="mt-1 text-2xl font-black">{value}</p></article>)}</section>}
function Activity(){const {state}=useDemo();return <section className="card p-5"><h2 className="text-xl font-black">Actividad reciente</h2><div className="mt-3 divide-y">{state.administration.audit.slice(0,5).map(e=><article className="py-3" key={e.id}><strong>{e.action}</strong><p className="text-sm text-slate-600">{e.detail}</p><small className="text-slate-500">{e.actor}</small></article>)}</div></section>}

export function StudentRoster(){const {state,adminUpdateStudent,adminAddStudent}=useDemo(),[query,setQuery]=useState(""),[filter,setFilter]=useState("all"),[notice,setNotice]=useState(""),[csv,setCsv]=useState("student_code,first_name,last_name,grade\nPK-12001,Ana,Pérez,4.º A"),[preview,setPreview]=useState<string[]>([]);const list=state.students.filter(s=>(filter==="all"||s.status===filter)&&`${s.firstName} ${s.lastName} ${s.code}`.toLowerCase().includes(query.toLowerCase()));const change=(s:DemoStudent,status:DemoStudent["status"])=>{adminUpdateStudent("school_admin",{...s,status});setNotice(`${s.preferredName}: ${statusLabel[status]}.`)};const parse=()=>{const rows=csv.trim().split(/\r?\n/).slice(1),seen=new Set<string>();setPreview(rows.map((r,i)=>{const p=r.split(",");if(p.length!==4||p.some(x=>!x.trim()))return`Fila ${i+2}: rechazada, faltan campos.`;if(seen.has(p[0])||state.students.some(s=>s.code===p[0]))return`Fila ${i+2}: rechazada, código duplicado.`;seen.add(p[0]);return`Fila ${i+2}: válida · ${p[1]} ${p[2]}`}))};const valid=preview.filter(x=>x.includes("válida")).length;return <><Header eyebrow="Padrón escolar" title="Estudiantes" copy="Lista escalable con búsqueda y filtros. Códigos enmascarados; nunca se muestran contraseñas."/>{notice?<p role="status" className="mt-4 rounded-xl bg-emerald-50 p-3 font-bold text-emerald-800">{notice}</p>:null}<div className="mt-5 grid gap-3 sm:grid-cols-[1fr_auto]"><label><span className="sr-only">Buscar estudiante</span><input className="field" placeholder="Buscar por nombre o código" value={query} onChange={e=>setQuery(e.target.value)}/></label><select className="field sm:w-48" aria-label="Filtrar estado" value={filter} onChange={e=>setFilter(e.target.value)}><option value="all">Todos</option><option value="active">Activos</option><option value="inactive">Inactivos</option><option value="archived">Archivados</option></select></div><section className="mt-4 grid gap-3">{list.map(s=><article className="card flex flex-wrap items-center justify-between gap-4 p-4" key={s.id}><div><h2 className="font-black">{s.firstName} {s.lastName}</h2><p className="text-sm text-slate-500">{s.grade} · Credencial {s.status==="active"?"habilitada":"no habilitada"} · Código •••{s.code.slice(-3)} · <span className={badge(s.status)}>{statusLabel[s.status]}</span></p></div><div className="flex flex-wrap gap-2"><button className="btn-secondary" onClick={()=>{adminUpdateStudent("school_admin",{...s,grade:s.grade==="5.º A"?"5.º B":"5.º A"});setNotice(`Grado de ${s.preferredName} actualizado.`)}}>Editar grado</button><button className="btn-secondary" onClick={()=>{const next=`PK-${String(Math.floor(10000+Math.random()*89999))}`;adminUpdateStudent("school_admin",{...s,code:next});setNotice("Código regenerado. El código anterior quedó invalidado.")}}>Regenerar código</button>{s.status==="active"?<><button className="btn-secondary" onClick={()=>confirm(`¿Desactivar a ${s.preferredName}? Se bloqueará su verificación.`)&&change(s,"inactive")}>Desactivar</button><button className="btn-secondary" onClick={()=>confirm(`¿Archivar a ${s.preferredName}?`)&&change(s,"archived")}>Archivar</button></>:<button className="btn-secondary" onClick={()=>change(s,"active")}>Activar</button>}<button className="btn-secondary" onClick={()=>setNotice("Restablecimiento iniciado. No se muestra ninguna contraseña anterior.")}>Restablecer acceso</button></div></article>)}</section><section className="card mt-6 p-5"><h2 className="text-xl font-black">Importar CSV</h2><p className="mt-1 text-sm text-slate-600">Plantilla: `student_code,first_name,last_name,grade`. La vista previa valida campos y duplicados antes de aplicar.</p><textarea className="field mt-4 min-h-32 font-mono text-sm" value={csv} onChange={e=>setCsv(e.target.value)} aria-label="Contenido CSV"/><button className="btn mt-3" onClick={parse}>Previsualizar importación</button>{preview.length?<><p className="mt-4 font-bold" role="status">Resumen: {valid} para agregar · 0 para actualizar · 0 omitidas · {preview.length-valid} rechazadas.</p><ul className="mt-2 space-y-2">{preview.map(x=><li className="rounded-xl bg-slate-50 p-3 text-sm" key={x}>{x}</li>)}</ul></>:null}<button className="btn-secondary mt-3" disabled={!valid||state.students.some(s=>s.code==="PK-12001")} onClick={()=>{adminAddStudent("school_admin",{firstName:"Ana",lastName:"Pérez",preferredName:"Ana",grade:"4.º A",code:"PK-12001",dailyLimit:300,perPurchaseLimit:200,allergies:[],blocked:[]});setNotice("Importación demo: 1 agregada, 0 actualizadas, 0 omitidas, 0 rechazadas.")}}>Aplicar filas válidas</button></section></>}

export function AdminUsers({kind}:{kind:"school"|"pos"}){const {state,adminAddUser,adminSetUserStatus}=useDemo(),[notice,setNotice]=useState("");const role=kind==="school"?"school_admin":"pos_operator",actor=kind==="school"?"school_admin":"cafeteria_admin";const invite=()=>{const count=state.administration.users.filter(u=>u.role===role).length+1;const ok=adminAddUser(actor,{name:kind==="school"?`Administrador invitado ${count}`:`Caja invitada ${count}`,email:kind==="school"?`admin${count}@demo.pikas.do`:`caja${count}@demo.pikas.do`,role,status:"inactive",scope:kind==="school"?"Instituto Nueva Generación":"Caja por asignar"});setNotice(ok?"Invitación demo creada. No se generó ni mostró una contraseña.":"No tienes permiso para invitar esta cuenta.")};return <><Header eyebrow="Cuentas y permisos" title={kind==="school"?"Administradores escolares":"Personal de caja"} copy="Las acciones emiten invitaciones o restablecimientos; nunca revelan contraseñas existentes."/><button className="btn mt-5" onClick={invite}>{kind==="school"?"Invitar administrador":"Invitar personal de caja"}</button><section className="mt-4 grid gap-3">{state.administration.users.filter(u=>u.role===role).map(u=><article className="card flex flex-wrap items-center justify-between gap-4 p-4" key={u.id}><div><h2 className="font-black">{u.name}</h2><p className="text-sm text-slate-500">{u.email} · {u.scope} · {u.lastActivity}</p><span className={badge(u.status)}>{statusLabel[u.status]}</span></div><div className="flex flex-wrap gap-2"><button className="btn-secondary" onClick={()=>setNotice(`Restablecimiento demo iniciado para ${u.name}.`)}>Restablecer acceso</button><button className="btn-secondary" onClick={()=>{const next=u.status==="active"?"suspended":"active";if(next!=="active"&&!confirm(`¿Suspender a ${u.name}? Perderá acceso a nuevas operaciones.`))return;const ok=adminSetUserStatus(actor,u.id,next);setNotice(ok?`${u.name}: ${statusLabel[next]}.`:"No se puede desactivar el último administrador escolar activo.")}}>{u.status==="active"?"Suspender":"Activar"}</button></div></article>)}</section>{notice?<p role="status" className="mt-4 rounded-xl bg-blue-50 p-3 font-bold text-blue-800">{notice}</p>:null}</>}

export function Partnerships({kind}:{kind:"school"|"cafeteria"}){const {state,adminSetPartnership}=useDemo(),role=kind==="school"?"school_admin":"cafeteria_admin";return <><Header eyebrow="Límite de datos" title={kind==="school"?"Cafeterías conectadas":"Escuelas conectadas"} copy="Una conexión autoriza operaciones mínimas; nunca concede acceso al padrón completo."/><section className="mt-5 grid gap-4">{state.administration.partnerships.map(p=><article className="card p-5" key={p.id}><div className="flex flex-wrap items-start justify-between gap-3"><div><h2 className="text-lg font-black">{p.schoolName} ↔ {p.cafeteriaName}</h2><p className="text-sm text-slate-500">{p.location} · solicitó {p.requestedBy==="school"?"escuela":"cafetería"}</p></div><span className={badge(p.status)}>{statusLabel[p.status]}</span></div><p className="mt-3 text-sm"><strong>Alcance:</strong> {p.scope.join(", ")}. Sin contactos familiares, credenciales ni navegación del padrón.</p><div className="mt-4 flex flex-wrap gap-2">{kind==="school"?(["active","rejected","suspended","revoked"] as PartnershipStatus[]).map(s=><button key={s} className="btn-secondary" disabled={p.status===s} onClick={()=>confirm(`¿Cambiar la conexión a ${statusLabel[s]}? Esto afecta verificación y checkout.`)&&adminSetPartnership(role,p.id,s)}>{s==="active"?"Aprobar/reactivar":statusLabel[s]}</button>):<button className="btn-secondary" disabled={p.status==="pending"} onClick={()=>adminSetPartnership(role,p.id,"pending")}>Solicitar conexión</button>}</div></article>)}</section></>}

export function MenuAdmin(){const {state,adminAddMenu,adminUpdateMenu}=useDemo(),[notice,setNotice]=useState("");const add=()=>{if(state.menuItems.some(i=>i.id==="menu-fruit")){setNotice("La fruta demo ya existe.");return}adminAddMenu("cafeteria_admin",{id:"menu-fruit",name:"Vaso de frutas",description:"Frutas frescas · Ingredientes: piña, sandía y melón",category:"Merienda",priceMinor:9500,allergens:[],available:true});setNotice("Producto ficticio añadido en unidades menores: RD$95.00.")};return <><Header eyebrow="Fuente compartida" title="Menú y productos" copy="Precio, categoría, ingredientes, alérgenos y disponibilidad se comparten con Estudiante y POS."/><div className="mt-5 flex flex-wrap gap-2"><button className="btn" onClick={add}>Añadir producto demo</button><a className="btn-secondary" href="/estudiante/menu">Previsualizar como Estudiante</a><a className="btn-secondary" href="/pos">Previsualizar en POS</a></div><section className="mt-4 grid gap-4 md:grid-cols-2">{state.menuItems.map(item=><article className="card p-5" key={item.id}><div className="flex justify-between gap-3"><div><h2 className="font-black">{item.name}</h2><p className="text-sm text-slate-500">{item.category} · {item.description}</p></div><strong>{money(item.priceMinor)}</strong></div><p className="mt-3 text-sm">Alérgenos/advertencias: {item.allergens.join(", ")||"Ninguno"}</p><div className="mt-4 flex flex-wrap gap-2"><button className="btn-secondary" onClick={()=>confirm(`¿Cambiar disponibilidad de ${item.name}? Afectará pedidos inmediatamente.`)&&adminUpdateMenu("cafeteria_admin",{...item,available:!item.available})}>{item.available?"Marcar no disponible":"Activar"}</button><button className="btn-secondary" onClick={()=>confirm(`¿Aumentar ${item.name} en RD$5?`)&&adminUpdateMenu("cafeteria_admin",{...item,priceMinor:item.priceMinor+500})}>+ RD$5</button><button className="btn-secondary" onClick={()=>{adminUpdateMenu("cafeteria_admin",{...item,category:item.category==="Merienda"?"Almuerzo":"Merienda",description:`${item.description} · Ingredientes revisados`,allergens:item.allergens.length?[]:["Gluten"]});setNotice(`Detalles, categoría y advertencias de ${item.name} actualizados.`)}}>Editar detalles demo</button></div></article>)}</section>{notice?<p role="status" className="mt-4 rounded-xl bg-emerald-50 p-3 font-bold text-emerald-800">{notice}</p>:null}</>}
export function TransactionsAdmin(){const {state}=useDemo(),[query,setQuery]=useState(""),[operator,setOperator]=useState("all"),[status,setStatus]=useState("completed"),[period,setPeriod]=useState("all");const rows=useMemo(()=>state.purchases.filter(p=>`${p.studentName} ${p.employeeLabel}`.toLowerCase().includes(query.toLowerCase())&&(operator==="all"||p.employeeLabel.includes(operator))),[state.purchases,query,operator]);return <><Header eyebrow="Ámbito cafetería" title="Transacciones" copy="Filtros por fecha, caja y estado; solo contexto mínimo, nunca contactos familiares o perfiles completos."/><div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><input className="field" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Estudiante u operador" aria-label="Filtrar transacciones"/><select className="field" value={period} onChange={e=>setPeriod(e.target.value)} aria-label="Filtrar fecha"><option value="all">Cualquier fecha</option><option value="today">Hoy</option></select><select className="field" value={operator} onChange={e=>setOperator(e.target.value)} aria-label="Filtrar operador"><option value="all">Todas las cajas</option><option value="Caja 1">Caja 1</option></select><select className="field" value={status} onChange={e=>setStatus(e.target.value)} aria-label="Filtrar estado"><option value="completed">Completadas</option></select></div><section className="card mt-4 divide-y p-4" data-period={period} data-status={status}>{rows.length?rows.map(p=><article className="flex justify-between gap-4 py-3" key={p.id}><div><strong>{p.studentName}</strong><p className="text-sm text-slate-500">Identificador limitado · {p.employeeLabel} · Completada</p></div><strong>{money(p.totalMinor)}</strong></article>):<p className="p-5 text-center text-slate-500">Aún no hay transacciones POS en este navegador.</p>}</section></>}
export function AuditPage(){return <><Header eyebrow="Trazabilidad" title="Actividad administrativa" copy="Cada cambio importante de cuenta, estudiante, menú o conexión deja un evento demo."/><div className="mt-5"><Activity/></div></>}
