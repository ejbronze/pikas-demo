import {NextRequest,NextResponse} from "next/server";import {isDemoMode} from "@/lib/env";import {createSupabaseServerClient} from "@/lib/supabase/server";export async function POST(req:NextRequest){if(!isDemoMode())try{await (await createSupabaseServerClient()).auth.signOut()}catch{}const r=NextResponse.redirect(new URL("/login",req.url),303);r.cookies.delete("pikas_demo_role");r.cookies.delete("pikas_demo_admin_role");return r}
