import {NextRequest,NextResponse} from "next/server";import {z} from "zod";import type {UserRole} from "@pikas/shared-types";import {isDemoMode} from "@/lib/env";import {createSupabaseServerClient} from "@/lib/supabase/server";import {createSupabaseAdminClient} from "@/lib/supabase/admin";
const schema=z.object({role:z.enum(["parent","student","pos","admin"]),identifier:z.string().trim().min(3).max(254),password:z.string().min(4).max(128)});const home=(role:UserRole)=>role==="parent"?"/familias":role==="student"?"/estudiante":`/${role}`;const fail=(req:NextRequest,code:string)=>NextResponse.redirect(new URL(`/login?error=${code}`,req.url),303);
export async function POST(req:NextRequest){const parsed=schema.safeParse(Object.fromEntries(await req.formData()));if(!parsed.success)return fail(req,"invalid");const {role,identifier,password}=parsed.data;if(isDemoMode()){const res=NextResponse.redirect(new URL(home(role),req.url),303);res.cookies.set("pikas_demo_role",role,{httpOnly:true,sameSite:"lax",secure:req.nextUrl.protocol==="https:",maxAge:60*60*8,path:"/"});return res}try{let email=identifier;if(role==="student"){const admin=createSupabaseAdminClient();const {data,error}=await admin.from("students").select("profiles!students_profile_id_fkey(email)").eq("student_code",identifier.toUpperCase()).eq("status","active").maybeSingle();const profile=Array.isArray(data?.profiles)?data?.profiles[0]:data?.profiles;if(error||!profile?.email)return fail(req,"credentials");email=profile.email}const supabase=await createSupabaseServerClient(),{data,error}=await supabase.auth.signInWithPassword({email,password});if(error||!data.user)return fail(req,"credentials");const {data:profile}=await supabase.from("profiles").select("role").eq("id",data.user.id).single();if(profile?.role!==role){await supabase.auth.signOut();return fail(req,"role")}return NextResponse.redirect(new URL(home(role),req.url),303)}catch{return fail(req,"config")}}
