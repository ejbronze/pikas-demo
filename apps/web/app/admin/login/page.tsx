import Link from "next/link";
import {BrandLogo} from "@/components/brand-logo";

export default async function AdminLogin({searchParams}:{searchParams:Promise<{error?:string}>}){const {error}=await searchParams;const message=error==="config"?"Supabase no está configurado. Define URL y anon key, o activa explícitamente el modo demo.":error?"No se pudo iniciar sesión con esa cuenta y alcance.":null;return <main className="grid min-h-screen place-items-center bg-pikas-navy p-4"><section className="w-full max-w-md rounded-3xl bg-white p-6 shadow-xl sm:p-8"><Link href="/" aria-label="PIKAS, inicio"><BrandLogo className="h-auto w-36" priority/></Link><span className="label mt-6 block">Acceso secundario</span><h1 className="mt-2 text-3xl font-black">Administración PIKAS</h1><p className="mt-2 text-sm text-slate-600">Entrada para administración escolar, cafetería y personal POS. La URL discreta no sustituye autorización.</p>{message?<p role="alert" className="mt-4 rounded-xl bg-red-50 p-3 text-sm font-bold text-red-800">{message}</p>:null}<form action="/api/auth/admin-login" method="post" className="mt-6 space-y-4"><label className="block font-bold">Correo demo<input className="field mt-2" name="identifier" type="email" defaultValue="admin.escuela@demo.pikas.do" required/></label><label className="block font-bold">Contraseña<input className="field mt-2" name="password" type="password" defaultValue="pikas-demo" required/></label><button className="btn w-full">Entrar a administración</button></form><p className="mt-5 rounded-xl bg-amber-50 p-3 text-xs leading-5 text-amber-950">Usa únicamente identidades ficticias de desarrollo o demostración.</p><Link href="/login" className="mt-5 inline-flex min-h-11 items-center font-bold text-blue-700">Volver a accesos principales</Link></section></main>}
