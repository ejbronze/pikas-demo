import {describe,expect,it} from "vitest";import {calculateBalance,canAccessRoute,canCancelPreorder,permittedStudentPatch,validatePreorder} from "./domain";
describe("ledger",()=>{it("uses completed credits and debits only",()=>expect(calculateBalance([{amountMinor:1000,direction:"credit",status:"completed"},{amountMinor:250,direction:"debit",status:"completed"},{amountMinor:900,direction:"credit",status:"pending"}])).toBe(750))});
describe("permissions",()=>{it("guards dashboards by role",()=>{expect(canAccessRoute("parent","/familias")).toBe(true);expect(canAccessRoute("student","/familias")).toBe(false);expect(canAccessRoute("student","/estudiante")).toBe(true)});it("limits student profile fields",()=>{expect(permittedStudentPatch({preferredName:"Sol"})).toBe(true);expect(permittedStudentPatch({dailyLimit:999})).toBe(false);expect(permittedStudentPatch({allergies:[]})).toBe(false)})});
describe("preorders",()=>{const valid={balanceMinor:1000,spentTodayMinor:100,dailyLimitMinor:800,perTransactionLimitMinor:500,priceMinor:200,allergens:[],studentAllergies:["Maní"],product:"Pasta",blockedProducts:[]};it("allows a valid order",()=>expect(validatePreorder(valid).ok).toBe(true));it("blocks allergies",()=>expect(validatePreorder({...valid,allergens:["Maní"]})).toEqual({ok:false,reason:"allergy"}));it("blocks products and limits",()=>{expect(validatePreorder({...valid,product:"Refresco",blockedProducts:["Refresco"]}).reason).toBe("blocked");expect(validatePreorder({...valid,priceMinor:1100,perTransactionLimitMinor:2000}).reason).toBe("balance")});it("only cancels early statuses",()=>{expect(canCancelPreorder("submitted")).toBe(true);expect(canCancelPreorder("preparing")).toBe(false)})});
